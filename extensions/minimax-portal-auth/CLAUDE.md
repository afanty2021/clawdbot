# MiniMax Portal Auth 扩展

[返回目录](../CLAUDE.md) > **extensions/minimax-portal-auth**

## 模块职责

MiniMax Portal OAuth 认证提供商插件。

## 功能概述

### OAuth 认证
- **Device Code 流程**：设备代码授权模式
- **双区域支持**：中国区和全球区
- **自动刷新**：支持刷新令牌
- **配置通知**：显示服务端通知消息

### MiniMax 模型
- **MiniMax M2.1**：标准模型
- **MiniMax M2.5**：推理模型（reasoning: true）

### 区域配置
- **CN（中国）**：`https://api.minimaxi.com/anthropic`
- **Global（全球）**：`https://api.minimax.io/anthropic`

## 关键依赖

### OAuth 实现
- `./oauth.ts` - OAuth 登录流程实现

### 配置常量
- **默认模型**：`MiniMax-M2.5`
- **上下文窗口**：200,000 tokens
- **最大令牌**：8,192
- **占位符**：`minimax-oauth`

## 对外接口

### 提供商配置
```typescript
{
  id: "minimax-portal",
  label: "MiniMax",
  aliases: ["minimax"],
  auth: [
    {
      id: "oauth",
      label: "MiniMax OAuth (Global)",
      hint: "Global endpoint - api.minimax.io",
      kind: "device_code",
    },
    {
      id: "oauth-cn",
      label: "MiniMax OAuth (CN)",
      hint: "CN endpoint - api.minimaxi.com",
      kind: "device_code",
    },
  ],
}
```

### 模型定义
```typescript
{
  id: "MiniMax-M2.1",
  name: "MiniMax M2.1",
  input: ["text"],
  reasoning: false,
  contextWindow: 200_000,
  maxTokens: 8192,
}

{
  id: "MiniMax-M2.5",
  name: "MiniMax M2.5",
  input: ["text"],
  reasoning: true,  // 推理模型
  contextWindow: 200_000,
  maxTokens: 8192,
}
```

### 认证结果
```typescript
{
  profiles: [{
    profileId: "minimax-portal:default",
    credential: {
      type: "oauth",
      provider: "minimax-portal",
      access: string,
      refresh: string,
      expires: number,
    },
  }],
  defaultModel: "minimax-portal/MiniMax-M2.5",
}
```

## 使用说明

### 认证流程
1. **选择区域**
   ```bash
   # 全球区
   openclaw providers add minimax-portal oauth

   # 中国区
   openclaw providers add minimax-portal oauth-cn
   ```

2. **设备授权**
   - 复制设备代码
   - 在浏览器中完成授权
   - 等待令牌自动交换

### 模型使用
```bash
# M2.1 标准模型
openclaw agent --model minimax-portal/MiniMax-M2.1

# M2.5 推理模型
openclaw agent --model minimax-portal/MiniMax-M2.5
```

### 别名配置
```typescript
{
  "minimax-portal/MiniMax-M2.1": { alias: "minimax-m2.1" },
  "minimax-portal/MiniMax-M2.5": { alias: "minimax-m2.5" },
}
```

## 注意事项

1. **令牌刷新**：MiniMax OAuth 令牌自动刷新
2. **刷新失败**：如果刷新失败或访问被撤销，重新登录
3. **Base URL**：默认使用区域特定的 URL，可覆盖配置
4. **服务端通知**：OAuth 可能返回通知消息，会显示给用户

## 相关文件

### 核心文件
- `index.ts` - 插件入口和 OAuth 流程
- `oauth.ts` - OAuth 登录流程实现

### 配置文件
- `package.json` - 插件清单

## 变更记录

### 2026-02-13 - 初始创建
- ✅ 创建 MiniMax Portal Auth 扩展文档
- 📋 记录 OAuth 认证流程和双区域支持
- 🔗 建立与 MiniMax 的关联


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

*No recent activity*
</claude-mem-context>