# Telegram 扩展 (extensions/telegram/)

[根目录](../../CLAUDE.md) > [extensions](../CLAUDE.md) > **telegram**

## 模块职责

提供 Telegram 平台的通信适配器，支持 Bot API、群组管理、频道操作和文件传输。

## 目录结构

```
extensions/telegram/
├── index.ts           # 插件入口
├── package.json       # 插件清单
└── src/
    ├── runtime.ts     # 运行时实现
    ├── monitor.ts     # 消息监控
    ├── send.ts        # 消息发送
    ├── targets.ts     # 目标解析
    ├── format.ts      # 消息格式化
    ├── accounts.ts    # 账户管理
    └── onboarding.ts  # 配置向导
```

## 入口与启动

### 启用插件
```bash
openclaw channels enable telegram
```

### 配置
```json
{
  "telegram": {
    "enabled": true,
    "botToken": "your-bot-token",
    "webhookUrl": "https://your-domain.com/webhook"
  }
}
```

## 对外接口

### TelegramRuntime 接口
```typescript
interface TelegramRuntime {
  monitor: ChannelMonitor;
  sender: MessageSender;
  targets: TargetResolver;
  accounts: AccountManager;
}
```

## 关键功能

### 消息收发
```typescript
// 发送消息
await telegram.send(target, {
  text: "Hello, Telegram!",
  parseMode: "Markdown",
});

// 发送图片
await telegram.sendPhoto(target, {
  photo: "./image.jpg",
  caption: "Image caption",
});

// 发送键盘
await telegram.sendKeyboard(target, {
  text: "Choose:",
  keyboard: [["Option 1"], ["Option 2"]],
});
```

### 群组管理
```typescript
// 获取群组信息
const chat = await telegram.getChat(chatId);

// 获取群组成员
const members = await telegram.getChatMembers(chatId);
```

## 测试

### 测试命令
```bash
pnpm test extensions/telegram
```

## 常见问题 (FAQ)

### Q: 如何获取 Bot Token？
A: 通过 @BotFather 创建机器人并获取 Token。

### Q: 支持哪些消息类型？
A: 支持文本、图片、视频、音频、文档、位置、联系人和投票。

### Q: Webhook 和 Long Polling 如何选择？
A: 生产环境推荐 Webhook，开发环境可使用 Long Polling。

## 相关文件

- `extensions/telegram/src/runtime.ts`
- `extensions/telegram/src/monitor.ts`

## 变更记录

### 2026-02-10 - 创建 Telegram 扩展文档
- ✅ 创建 `extensions/telegram/CLAUDE.md` 文档
- 📋 记录 Bot API 和消息类型
- 🔗 建立 Telegram 适配器导航


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Feb 10, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #2211 | 10:28 AM | 🟣 | Created Telegram extension documentation with Bot API details | ~307 |
</claude-mem-context>