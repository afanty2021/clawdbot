# Lobster 扩展

[返回目录](../CLAUDE.md) > **extensions/lobster**

## 模块职责

Lobster 工作流工具插件，提供类型化管道和可恢复审批功能。

## 功能概述

### 工作流特性
- **类型化管道**：强类型的工作流管道
- **可恢复审批**：支持中断后恢复的审批流程
- **沙箱检测**：沙箱环境下返回 null
- **可选注册**：`optional: true` 标记

### 工具集成
- **createLobsterTool**：创建 Lobster 工具
- **AnyAgentTool**：通用代理工具接口

## 关键依赖

### OpenClaw 类型系统
- `AnyAgentTool` - 通用代理工具接口
- `OpenClawPluginApi` - 插件 API 接口
- `OpenClawPluginToolFactory` - 工具工厂接口

## 对外接口

### 工具注册
```typescript
api.registerTool(
  ((ctx) => {
    if (ctx.sandboxed) {
      return null;
    }
    return createLobsterTool(api) as AnyAgentTool;
  }) as OpenClawPluginToolFactory,
  { optional: true },
);
```

### 工具实现
```typescript
// src/lobster-tool.ts
export function createLobsterTool(api: OpenClawPluginApi): AnyAgentTool;
```

## 使用说明

### 自动加载
插件注册后自动创建工具，无需额外配置。

### 沙箱检测
```typescript
if (ctx.sandboxed) {
  return null;  // 沙箱环境下不提供工具
}
```

### 工作流示例
```typescript
// 类型化管道
const pipeline = lobster
  .pipe(step1)
  .pipe(step2)
  .pipe(step3);

// 可恢复审批
await lobster.approve(pipelineId);
```

## 注意事项

1. **沙箱限制**：沙箱环境下工具不可用
2. **类型安全**：管道在编译时进行类型检查
3. **审批恢复**：审批流程中断后可以恢复

## 相关文件

### 核心文件
- `index.ts` - 插件入口
- `src/lobster-tool.ts` - 工具实现

### 配置文件
- `package.json` - 插件清单

## 变更记录

### 2026-02-13 - 初始创建
- ✅ 创建 Lobster 扩展文档
- 📋 记录工作流特性和工具注册
- 🔗 建立与工作流处理的关联


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

*No recent activity*
</claude-mem-context>