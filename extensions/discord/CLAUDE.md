# Discord 扩展 (extensions/discord/)

[根目录](../../CLAUDE.md) > [extensions](../CLAUDE.md) > **discord**

## 模块职责

提供 Discord 平台的通信适配器，支持消息收发、反应管理、文件上传和频道操作。

## 目录结构

```
extensions/discord/
├── index.ts           # 插件入口
├── package.json       # 插件清单
└── src/
    ├── runtime.ts     # 运行时实现
    ├── monitor.ts     # 消息监控
    ├── send.ts        # 消息发送
    ├── targets.ts     # 目标解析
    ├── format.ts      # 消息格式化
    ├── accounts.ts    # 账户管理
    └── onboarding.ts  # 配置向导
```

## 入口与启动

### 启用插件
```bash
openclaw channels enable discord
```

### 配置
```json
{
  "discord": {
    "enabled": true,
    "token": "your-bot-token",
    "serverId": "your-server-id"
  }
}
```

## 对外接口

### DiscordRuntime 接口
```typescript
interface DiscordRuntime {
  monitor: ChannelMonitor;
  sender: MessageSender;
  targets: TargetResolver;
  accounts: AccountManager;
}
```

## 关键功能

### 消息收发
```typescript
// 发送消息
await discord.send(target, {
  content: "Hello, Discord!",
  replyTo: messageId,
});

// 发送反应
await discord.react(target, messageId, "👍");
```

### 频道管理
```typescript
// 获取频道列表
const channels = await discord.getChannels();

// 发送到频道
await discord.sendToChannel(channelId, message);
```

## 测试

### 测试命令
```bash
pnpm test extensions/discord
```

## 常见问题 (FAQ)

### Q: 如何获取 Bot Token？
A: 在 Discord Developer Portal 创建应用并生成 Bot Token。

### Q: 需要哪些权限？
A: 需要 `MESSAGES`, `MESSAGE_CONTENT`, `ADD_REACTIONS` 等权限。

## 相关文件

- `extensions/discord/src/runtime.ts`
- `extensions/discord/src/monitor.ts`

## 变更记录

### 2026-02-10 - 创建 Discord 扩展文档
- ✅ 创建 `extensions/discord/CLAUDE.md` 文档
- 📋 记录消息收发和频道管理
- 🔗 建立 Discord 适配器导航


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Feb 10, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #2210 | 10:27 AM | 🟣 | Created first extension-specific CLAUDE.md for Discord adapter | ~370 |
</claude-mem-context>