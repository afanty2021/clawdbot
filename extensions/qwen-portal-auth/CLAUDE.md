# Qwen Portal Auth 扩展

[返回目录](../CLAUDE.md) > **extensions/qwen-portal-auth**

## 模块职责

Qwen（通义千问）Portal OAuth 认证提供商插件。

## 功能概述

### OAuth 认证
- **Device Code 流程**：设备代码授权模式
- **免费层支持**：支持 Qwen 免费层模型
- **自动刷新**：支持刷新令牌

### Qwen 模型
- **coder-model**：Qwen Coder（编程模型）
- **vision-model**：Qwen Vision（视觉模型，支持文本和图像）

## 关键依赖

### OAuth 实现
- `./oauth.ts` - OAuth 登录流程实现

### 配置常量
- **默认模型**：`qwen-portal/coder-model`
- **默认 URL**：`https://portal.qwen.ai/v1`
- **上下文窗口**：128,000 tokens
- **最大令牌**：8,192
- **占位符**：`qwen-oauth`

## 对外接口

### 提供商配置
```typescript
{
  id: "qwen-portal",
  label: "Qwen",
  aliases: ["qwen"],
  auth: [{
    id: "device",
    label: "Qwen OAuth",
    hint: "Device code login",
    kind: "device_code",
  }],
}
```

### 模型定义
```typescript
{
  id: "coder-model",
  name: "Qwen Coder",
  input: ["text"],
  reasoning: false,
  contextWindow: 128_000,
  maxTokens: 8192,
}

{
  id: "vision-model",
  name: "Qwen Vision",
  input: ["text", "image"],
  reasoning: false,
  contextWindow: 128_000,
  maxTokens: 8192,
}
```

### 认证结果
```typescript
{
  profiles: [{
    profileId: "qwen-portal:default",
    credential: {
      type: "oauth",
      provider: "qwen-portal",
      access: string,
      refresh: string,
      expires: number,
    },
  }],
  defaultModel: "qwen-portal/coder-model",
}
```

## 使用说明

### 认证流程
1. **启动认证**
   ```bash
   openclaw providers add qwen-portal
   ```

2. **设备授权**
   - 复制设备代码
   - 在浏览器中完成授权
   - 等待令牌自动交换

### 模型使用
```bash
# Qwen Coder（默认）
openclaw agent --model qwen-portal/coder-model

# Qwen Vision（支持图像）
openclaw agent --model qwen-portal/vision-model

# 使用别名
openclaw agent --model qwen
```

### 别名配置
```typescript
{
  "qwen-portal/coder-model": { alias: "qwen" },
  "qwen-portal/vision-model": {},
}
```

### 自定义 Base URL
```bash
# 可选：自定义 API 端点
openclaw config set models.providers.qwen-portal.baseUrl https://custom.url/v1
```

## 注意事项

1. **令牌刷新**：Qwen OAuth 令牌自动刷新
2. **刷新失败**：如果刷新失败或访问被撤销，重新登录
3. **Portal 访问**：确保 Qwen 账户具有 Portal 访问权限
4. **免费层**：支持 Qwen 免费层模型

## 相关文件

### 核心文件
- `index.ts` - 插件入口
- `oauth.ts` - OAuth 流程实现

### 配置文件
- `openclaw.plugin.json` - 插件清单
- `package.json` - 插件清单

## 变更记录

### 2026-02-13 - 初始创建
- ✅ 创建 Qwen Portal Auth 扩展文档
- 📋 记录 OAuth 认证流程
- 🔗 建立与 Qwen 的关联


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

*No recent activity*
</claude-mem-context>