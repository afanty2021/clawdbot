# Copilot Proxy 扩展

[返回目录](../CLAUDE.md) > **extensions/copilot-proxy**

## 模块职责

GitHub Copilot 代理提供商插件，提供本地 Copilot 代理服务器的集成支持。

## 功能概述

### 本地代理支持
- **VS Code LM**：VS Code 语言模型代理
- **多模型支持**：GPT-5.x、Claude Opus 4.x、Gemini 3.x、Grok Code
- **自定义配置**：支持自定义基础 URL 和模型 ID
- **零成本**：输入输出成本设为 0（本地代理）

### 默认模型
- `gpt-5.2`、`gpt-5.2-codex`
- `gpt-5.1`、`gpt-5.1-codex`、`gpt-5.1-codex-max`
- `gpt-5-mini`
- `claude-opus-4.6`、`claude-opus-4.5`
- `claude-sonnet-4.5`、`claude-haiku-4.5`
- `gemini-3-pro`、`gemini-3-flash`
- `grok-code-fast-1`

### 认证配置
- **认证类型**：`token`
- **默认令牌**：`n/a`（本地代理无需真实令牌）
- **基础 URL**：默认 `http://localhost:3000/v1`
- **API 类型**：`openai-completions`

## 对外接口

### 提供商配置
```typescript
{
  id: "copilot-proxy",
  label: "Copilot Proxy",
  auth: [{
    id: "local",
    label: "Local proxy",
    kind: "custom",
  }],
  models: {
    providers: {
      "copilot-proxy": {
        baseUrl: string,      // 代理服务器地址
        apiKey: string,        // 固定为 "n/a"
        api: "openai-completions",
        authHeader: false,     // 不发送认证头
        models: ModelDefinition[],
      },
    },
  },
}
```

### 模型定义
```typescript
{
  id: string,              // 模型 ID
  name: string,            // 显示名称
  api: "openai-completions",
  reasoning: false,
  input: ["text", "image"],
  cost: {
    input: 0,
    output: 0,
    cacheRead: 0,
    cacheWrite: 0,
  },
  contextWindow: 128_000,
  maxTokens: 8192,
}
```

## 关键依赖

### OpenClaw Plugin SDK
- `openclaw/plugin-sdk` - 插件开发工具包
- `emptyPluginConfigSchema` - 空配置模式

### 配置验证
- **URL 验证**：确保输入有效的 URL
- **模型验证**：至少需要提供一个模型 ID

## 使用说明

### 配置步骤
1. 启动 VS Code Copilot Proxy 扩展
2. 运行 `openclaw providers add copilot-proxy`
3. 输入代理服务器 URL（默认 `http://localhost:3000/v1`）
4. 输入模型 ID（逗号分隔）
5. 完成配置

### 命令示例
```bash
# 添加 Copilot 代理
openclaw providers add copilot-proxy

# 使用模型
openclaw agent --message "Hello" --model copilot-proxy/gpt-5.2
```

## 注意事项

1. **Copilot 扩展**：使用前必须启动 VS Code Copilot Proxy 扩展
2. **URL 格式**：基础 URL 必须包含 `/v1` 路径
3. **模型可用性**：模型可用性取决于 Copilot 订阅计划
4. **成本设置**：本地代理成本设为 0，实际成本由 Copilot 订阅承担

## 相关文件

### 核心文件
- `index.ts` - 插件入口和提供商注册

### 配置文件
- `package.json` - 插件清单

## 变更记录

### 2026-02-13 - 初始创建
- ✅ 创建 Copilot Proxy 扩展文档
- 📋 记录本地代理配置流程
- 🔗 建立与 VS Code Copilot Proxy 的关联


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

*No recent activity*
</claude-mem-context>