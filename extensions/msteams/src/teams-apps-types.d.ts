/**
 * Type declarations for @microsoft/teams.apps
 * These types are not properly exported by the package, so we declare them here.
 */

declare module "@microsoft/teams.apps" {
  export interface IHttpServerAdapter {
    // Add required properties and methods based on actual usage
    [key: string]: unknown;
  }
}
