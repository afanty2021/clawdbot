# 技能系统模块 (src/agents/skills/)

[根目录](../../CLAUDE.md) > [src](../CLAUDE.md) > [agents](./CLAUDE.md) > **skills**

## 模块职责

提供 AI 代理技能（Skills）的加载、管理和执行能力。技能是预定义的任务模板，可以让 AI 代理执行特定领域的任务，如代码审查、PR 准备、问题诊断等。

## 目录结构

```
src/agents/skills/
├── bundled/             # 内置技能目录
│   ├── skill-name-1/
│   ├── skill-name-2/
│   └── ...
├── workspace.ts         # 工作区技能管理
├── loader.ts           # 技能加载器
├── frontmatter.ts      # 技能元数据解析
├── install.ts          # 技能安装
├── status.ts           # 技能状态查询
├── config.ts           # 技能配置
├── types.ts            # 技能类型定义
└── test/
```

## 入口与启动

### 主入口
- **`src/agents/skills/loader.ts`** - 技能加载器
- **`src/agents/skills/workspace.ts`** - 工作区技能管理器

### 加载流程
```typescript
import { SkillsLoader } from "./skills/loader.ts";

const loader = new SkillsLoader();
const skills = await loader.loadAll();
```

## 对外接口

### SkillsLoader 接口
```typescript
interface SkillsLoader {
  loadAll(): Promise<Skill[]>;
  load(id: string): Promise<Skill | null>;
  reload(id: string): Promise<Skill>;
  getInstalled(): Skill[];
}
```

### Skill 接口
```typescript
interface Skill {
  id: string;
  name: string;
  description: string;
  version: string;
  category: SkillCategory;
  parameters: SkillParameter[];
  execute(params: SkillParams): Promise<SkillResult>;
  validate(params: SkillParams): ValidationResult;
}
```

### SkillCategory 枚举
```typescript
enum SkillCategory {
  CODE_REVIEW = "code_review",
  PR_PREPARE = "pr_prepare",
  DEBUGGING = "debugging",
  DOCUMENTATION = "documentation",
  TESTING = "testing",
  REFACTORING = "refactoring",
  GENERAL = "general",
}
```

## 子模块详解

### 1. 内置技能 (`bundled/`)

**职责**：提供开箱即用的预定义技能

**常见技能**：
- `code-review` - 代码审查技能
- `prepare-pr` - PR 准备技能
- `review-pr` - PR 审查技能
- `merge-pr` - PR 合并技能

### 2. 技能加载 (`loader.ts`)

**职责**：从文件系统加载和解析技能定义

**加载流程**：
1. 扫描技能目录
2. 解析 `skill.json` 或 `skill.yaml`
3. 验证技能 Schema
4. 注册到技能注册表

### 3. 工作区技能 (`workspace.ts`)

**职责**：管理工作区级别的技能

**功能**：
- 加载工作区技能配置
- 合并内置和工作区技能
- 同步技能到目标工作区

### 4. 技能元数据 (`frontmatter.ts`)

**职责**：解析和管理技能的前置元数据

**元数据格式**：
```yaml
---
name: skill-name
description: 技能描述
version: 1.0.0
category: code_review
parameters:
  - name: target
    type: string
    required: true
  - name: options
    type: object
---
```

### 5. 技能安装 (`install.ts`)

**职责**：技能的安装、升级和卸载

**功能**：
- 从远程安装技能
- 更新技能版本
- 卸载技能
- 验证技能完整性

### 6. 技能状态 (`status.ts`)

**职责**：查询技能的运行状态和使用统计

**状态信息**：
- 最后执行时间
- 执行次数
- 成功率
- 平均执行时间

## 关键依赖与配置

### 配置文件
```yaml
# skills.yaml
skills:
  enabled:
    - code-review
    - prepare-pr
  disabled: []
  workspace:
    enabled: true
    path: .openclaw/skills
```

### 环境变量
```bash
SKILLS_DIR           # 技能目录
SKILLS_BUNDLED_PATH  # 内置技能路径
```

## 测试与质量

### 测试文件
- `src/agents/skills/**/*.test.ts`

### 测试命令
```bash
pnpm test src/agents/skills
```

## 常见问题 (FAQ)

### Q: 如何创建新技能？
A: 在 `skills/` 目录创建新文件夹，添加 `skill.json` 定义文件。

### Q: 技能如何传递参数？
A: 在 `skill.json` 中定义参数 Schema，执行时传入 `SkillParams` 对象。

### Q: 工作区技能和内置技能优先级？
A: 工作区技能覆盖同名内置技能。

## 相关文件清单

### 核心文件
- `src/agents/skills/loader.ts` - 技能加载器
- `src/agents/skills/workspace.ts` - 工作区管理
- `src/agents/skills/install.ts` - 安装逻辑

### 类型文件
- `src/agents/skills/types.ts` - 类型定义

### CLI 文件
- `src/cli/skills-cli.ts` - CLI 命令

## 变更记录

### 2026-02-10 - 创建技能模块文档
- ✅ 创建 `src/agents/skills/CLAUDE.md` 文档
- 📋 记录技能加载和执行流程
- 🔗 建立技能系统导航


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Feb 10, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #2212 | 10:30 AM | 🟣 | Documentation coverage campaign achieved 100% core module coverage | ~546 |
| #2207 | 10:25 AM | 🟣 | Documentation coverage significantly improved - 10 new CLAUDE.md files created | ~538 |
</claude-mem-context>