# Cron CLI 模块 (src/cli/cron-cli/)

[根目录](../../CLAUDE.md) > [src](../CLAUDE.md) > [cli](./CLAUDE.md) > **cron-cli**

## 模块职责

提供定时任务相关的命令行接口，支持任务的创建、查看、删除、暂停和恢复。该模块是 `src/cron/` 服务层的 CLI 前端。

## 目录结构

```
src/cli/cron-cli/
├── cron-cli.ts         # CLI 入口
├── cron-cli.test.ts    # 测试文件
└── cron-cli.coverage.test.ts  # 覆盖率测试
```

## 入口与启动

### CLI 使用
```bash
openclaw cron --help
```

## 命令列表

### 基础命令
```bash
# 列出所有任务
openclaw cron list

# 查看任务详情
openclaw cron show <job-id>

# 创建任务
openclaw cron create --expression "*/5 * * * *" --type agent --payload '{"agent": "test"}'

# 删除任务
openclaw cron delete <job-id>

# 暂停任务
openclaw cron pause <job-id>

# 恢复任务
openclaw cron resume <job-id>
```

### 选项
```bash
--verbose, -v     # 详细输出
--json            # JSON 格式输出
--format          # 输出格式
```

## 子命令详解

### list - 列出任务
```bash
openclaw cron list [--status running|paused|completed]
```

### show - 查看详情
```bash
openclaw cron show <job-id> [--logs]
```

### create - 创建任务
```bash
openclaw cron create \
  --expression "0 * * * *" \
  --type agent_run \
  --payload '{"agent_id": "test-agent"}' \
  --name "每小时任务"
```

### delete - 删除任务
```bash
openclaw cron delete <job-id> [--force]
```

## 常见问题 (FAQ)

### Q: Cron 表达式如何编写？
A: 使用标准 5 字段格式：`分 时 日 月 周`

### Q: 如何查看任务执行日志？
A: 使用 `openclaw cron show <job-id> --logs`

### Q: 任务执行失败怎么办？
A: 检查 `--logs` 输出，或使用 `cron-cli` 的调试模式。

## 相关文件

### 核心文件
- `src/cli/cron-cli.ts` - CLI 入口
- `src/cron/` - Cron 服务层

## 变更记录

### 2026-02-10 - 创建 Cron CLI 文档
- ✅ 创建 `src/cli/cron-cli/CLAUDE.md` 文档
- 📋 记录命令用法和选项
- 🔗 建立 Cron CLI 导航


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Feb 10, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #2209 | 10:27 AM | 🟣 | Created CLI submodule documentation for cron and daemon management | ~348 |
</claude-mem-context>