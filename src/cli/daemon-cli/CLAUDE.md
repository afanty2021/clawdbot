# Daemon CLI 模块 (src/cli/daemon-cli/)

[根目录](../../CLAUDE.md) > [src](../CLAUDE.md) > [cli](./CLAUDE.md) > **daemon-cli**

## 模块职责

提供守护进程管理的命令行接口，支持守护进程的安装、启动、停止、状态查询和日志查看。该模块用于管理 OpenClaw 后台服务。

## 目录结构

```
src/cli/daemon-cli/
├── daemon-cli.ts       # CLI 入口
├── daemon-cli.test.ts # 测试文件
└── daemon-cli.coverage.test.ts  # 覆盖率测试
```

## 入口与启动

### CLI 使用
```bash
openclaw daemon --help
```

## 命令列表

### 基础命令
```bash
# 安装守护进程
openclaw daemon install

# 启动守护进程
openclaw daemon start

# 停止守护进程
openclaw daemon stop

# 重启守护进程
openclaw daemon restart

# 查看状态
openclaw daemon status

# 查看日志
openclaw daemon logs [--follow]

# 卸载守护进程
openclaw daemon uninstall
```

### 选项
```bash
--verbose, -v     # 详细输出
--force           # 强制执行
--user            # 用户级别安装
--system          # 系统级别安装
```

## 子命令详解

### install - 安装守护进程
```bash
openclaw daemon install \
  --user \
  --config ~/.config/openclaw \
  --data ~/.local/share/openclaw
```

### start - 启动服务
```bash
openclaw daemon start [--daemon]
```

### status - 查看状态
```bash
openclaw daemon status [--json]
```

### logs - 查看日志
```bash
openclaw daemon logs --follow --lines 100
```

## 常见问题 (FAQ)

### Q: 守护进程安装失败怎么办？
A: 检查权限，使用 `--verbose` 调试。

### Q: 如何查看详细日志？
A: 使用 `openclaw daemon logs --verbose`

### Q: 守护进程占用端口如何处理？
A: 使用 `openclaw daemon stop` 先停止，或修改配置文件中的端口。

## 相关文件

### 核心文件
- `src/cli/daemon-cli.ts` - CLI 入口
- `src/daemon/` - 守护进程服务层

### 相关模块
- `src/cli/gateway-cli/` - 网关 CLI
- `src/cli/node-cli/` - 节点 CLI

## 变更记录

### 2026-02-10 - 创建 Daemon CLI 文档
- ✅ 创建 `src/cli/daemon-cli/CLAUDE.md` 文档
- 📋 记录守护进程管理命令
- 🔗 建立 Daemon CLI 导航


<claude-mem-context>
# Recent Activity

<!-- This section is auto-generated by claude-mem. Edit content outside the tags. -->

### Feb 10, 2026

| ID | Time | T | Title | Read |
|----|------|---|-------|------|
| #2209 | 10:27 AM | 🟣 | Created CLI submodule documentation for cron and daemon management | ~348 |
</claude-mem-context>